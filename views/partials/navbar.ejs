<header class="navbar-fixed">
<nav class="blue navbar">
	<div class="nav-wrapper">
		<ul class="left">
			<li><a class="brand-logo" href="/">keyswig<i class="fa fa-car" aria-hidden="true"></i></a></li>
			<li><p>key.switch.ignition.</p></li>
		</ul>
		<ul class="right hide-on-small-and-down">
			<% if (currentUser) { %>
				<li><a href="/my-garage">My Garage</a></li>
				<li><a href="#" class="dropdown-button" data-activates="dropdown1" data-beloworigin="true" data-hover="true"><%= currentUser.displayname %><i class="material-icons right">arrow_drop_down</i></a></li>
					<ul id="dropdown1" class="blue-grey darken-1 dropdown-content">
						<li><a href="/" class="white-text">Browse</a></li>
						<li><a href="/account" class="white-text">Account</a></li>
						<li><a href="/logout" class="white-text">Logout</a></li>
					</ul>

			<% } else { %>
				<li><a href="/signup">Sign Up</a></li>
				<li><a href="/login">Login</a></li>
			<% } %>
		</ul>
	</div>
</nav>
</header>


<div class="flash">
	<% if (typeof message !== 'undefined') { %>
		<% if (typeof message.error !== 'undefined' && message.error.length > 0) { %>
		<div class="card-panel red"><%= message.error %></div>
		<% } %>
    <% if (typeof message.info !== 'undefined' && message.info.length > 0) { %>
    	<div class="card-panel yellow"><%= message.info %></div>
		<% } %>
    <% if (typeof message.success !== 'undefined' && message.success.length > 0) { %>
		<div class="card-panel green"><%= message.success %></div>
    	<% } %>
	<% } %>
</div>